'use strict';

require('should');

let Player = require('..');

describe('Bridge Player', () => {

    it('should be seated', done => {
        done('NYI');
    });

    it('should be a member of a team', done => {
        done('NYI');
    });

});
